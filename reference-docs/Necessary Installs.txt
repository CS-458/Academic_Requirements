General install:
npm install

For dropdown menu change:
npm install react-select

For semesters tables:
npm install react-table

For using conditional classNames
npm install clsx
(may need to also run the following: "npm install --save react react-dom @types/react @types/react-dom")

